# imgtoword
识别图片内容，转换成表格数据

## 编码约定

- 所有源码、配置和文档统一使用 UTF-8
- 新增或修改文本文件时不要改成 GBK / ANSI / 本地默认编码
- 如果编辑器提供自动识别编码，优先关闭自动猜测，避免误保存
## OpenRouter

- OpenRouter API Key 只会保存在当前浏览器本地
- 模型列表只保留支持图片输入的模型
- 会自动过滤 `openrouter/auto` 和免费模型
